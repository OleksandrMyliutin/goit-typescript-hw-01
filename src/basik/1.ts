const age: number = 50;
const username: string = 'Max';
const toggle: boolean = true;
const empty: null = null;
type A = {
    param: number;
}
const callback = (a: A) => { 
    return 100 + a.param;
};
